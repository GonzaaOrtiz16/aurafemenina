# Supabase (cliente público — la anon key está protegida por RLS)
# Copiá este archivo a .env y completá con los valores de tu proyecto.
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Secretos del backend (NO van acá ni en el frontend).
# Se configuran en Supabase > Edge Functions > Secrets:
#   LOVABLE_API_KEY
#   SUPABASE_SERVICE_ROLE_KEY
